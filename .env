# Silence cosmetic "Failed to parse source map" warnings from aws-amplify's
# bundled packages during `react-scripts start` / `build`. These are warnings,
# not errors; aws-amplify v6 otherwise builds cleanly on Create React App.
GENERATE_SOURCEMAP=false
