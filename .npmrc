# react-scripts@5 wants typescript@^4 while @aws-amplify/backend-cli wants
# typescript@^5. They can't both be satisfied, so use legacy peer resolution
# (npm ignores the conflict). Required for `npm ci` in the Amplify build.
legacy-peer-deps=true
